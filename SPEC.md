# BreakFit(休息練)— 規格書

> **2026-09-24 更新(優先於任何舊說法)**:使用者要求背景自動執行、用右下角托盤控制 →「開機自啟預設**開**」、`--hidden` 啟動不開視窗、關視窗=縮到托盤、托盤選單細節見 §8「托盤」。任何地方寫「開機自啟預設關」都已作廢。

Windows 桌面常駐程式。上班時段內每隔 N 分鐘強制全螢幕休息，把當天的無器械訓練菜單拆散到每次休息裡做；跳過或做不完的量滾到下一次，最後一次休息必須清空，否則當天不合格。附 tradervue 風格的每日記錄月曆。

菜單資料：`plan.json`(已由影片整理好，**以它為準，不要改內容**)。示範片：`assets/clips/<clip>.mp4`(另一條線在剪，檔名 = plan.json 的 `clip` 欄位；程式要能容忍檔案暫時不存在 → 顯示動作名稱佔位)。

## 技術選型(已定，不要換)

- Electron(最新穩定版)+ 純 HTML/CSS/JS,不要 React/Vue/打包器。`npm.cmd` / `npx.cmd`(Windows PowerShell 環境)。
- 邏輯模組 `src/core/*.js` 必須是純函式、不碰 Electron,讓 `node --test` 能直接測。
- 資料：`app.getPath('userData')/data.json`,寫檔用「寫 tmp → rename」避免損毀。
- 單一實例鎖(`requestSingleInstanceLock`)。
- 開機自啟：用 `app.setLoginItemSettings({ openAtLogin, args:['--hidden'] })`,**預設開**(使用者 2026-09-24 明確要求背景自動執行)。只在 `app.isPackaged` 時註冊(開發模式不要把 electron.exe 註冊進開機)。托盤選單與設定頁都能切換。
- **背景常駐**:程式是托盤程式。帶 `--hidden` 啟動(開機自啟)時不開任何視窗，只出現托盤圖示；手動雙擊 exe 啟動時開主視窗。關閉主視窗(X)= 隱藏到托盤，不結束；只有托盤選單「結束」才真的退出。`window-all-closed` 不可以 quit。
- 打包：`@electron/packager` → `dist/BreakFit-win32-x64/BreakFit.exe`。`npm run pack`。
- 字型/圖示：UI 圖示用 **Material Symbols**(npm `material-symbols` 套件，本地載入，離線可用),**禁止 emoji**。文字 Inter(npm `@fontsource/inter`)+ `"Microsoft JhengHei UI"` 後備。中等字重(500),單一強調色，說明文字越少越好。深色主題。
- 語言：介面全部繁體中文(台灣用語)。

## 1. 時間表(slots)

設定：`start`(預設 10:00)、`end`(預設 21:00)、`interval` 分鐘(預設 60)、`activeWeekdays`(預設週一到週五，可改)。

- 休息時段 `t_k = start + k × interval`,k = 1, 2, … 只要 `t_k ≤ end`。預設 → 11:00, 12:00, …, 21:00 共 11 個。
- 若一個都算不出來(interval > 時段長),就只有一個時段在 `end`。
- 最後一個 slot = **最後一次休息**。
- 非 active weekday → 當天沒有任何 slot,記錄為 `off`。

## 2. 菜單循環

- 循環 `[d1, d2, d3, rest]`,只在 active 日前進(週末不算進循環)。
- 設定裡的 `cycleAnchor = { date, index }`;某個 active 日 D 的 index = `(anchor.index + D 與 anchor 之間經過的 active 日數) mod 4`。
- 設定頁可「把今天設成 第1天/第2天/第3天/休息」→ 把 anchor 改成 today。
- 當天第一次建立 day record 時就把 planDay 寫死，之後改設定不影響已建立的過去日子。
- `rest` 日：不跳訓練畫面，月曆顯示灰色「休息」。

## 3. 把菜單分配到 slot

- 一個 unit = plan 裡一個動作(帶全部組數)或一輪腹肌循環。
- N 個 unit、M 個 slot:第 i 個 unit(0 起算)排到 slot `floor(i × M / N)`。M > N 時最後一個 slot 會空著，當作補做緩衝。
- 每次休息要做的量：**`pending(k)` = 所有「分配到的 slot ≤ k」且還有剩餘組數的 unit**。跳過、中途離開、錯過的量就是這樣自然滾到下一次。
- **最後一個 slot**:pending = 當天所有剩餘量。

## 4. 觸發

- 主程序每 15 秒檢查。`now ≥ t_k`、slot k 還沒處理、目前沒有休息畫面 → 開啟 slot k 的休息。
- 一次跨過好幾個 slot(睡眠、鎖定、程式晚開):比較早的標成 `missed`,只開最新的那個(它的 pending 自動包含前面的量)。監聽 `powerMonitor` 的 `resume` / `unlock-screen` 立刻檢查。
- 程式啟動時：已經過去超過 5 分鐘的 slot 標 `missed`,不補跳；5 分鐘內的照常跳。
- pending 為空的非最後 slot:不跳全螢幕，只發 Windows 通知「休息一下，起來走走」(可在設定關掉)。
- 當天全部做完後，剩下的 slot 都不再跳。
- 托盤選單「現在就休息」:立刻開一次休息，內容 = `pending(下一個 slot)`,不標記 slot;之後那個 slot 到了若已沒剩就不跳。

## 5. 休息畫面(overlay)流程 —— 像市面上的運動 app

全螢幕、無邊框、`setAlwaysOnTop(true, 'screen-saver')`、搶焦點。主螢幕放訓練畫面，其他螢幕蓋一層深色(顯示「休息中」)。

**版面固定(見 DESIGN.md §4)**:每個階段都是同一個兩欄畫面 —— 左邊是 16:9 示範片，位置與大小永遠不變(不縮小、不變角落小窗);右邊是資訊與所有按鈕，主按鈕永遠在右欄最底下同一個位置。自動倒數寫在主按鈕裡，主計時(休息、秒數型動作)是右欄的圓環。

1. **開場**:左邊播第一個動作的示範片；右邊是今天的訓練標題 + 這次的清單(動作、組數×次數，上次滾過來的用 history 圖示標出)。主按鈕 `開始` 內顯示 10 秒倒數，歸零自動開始。按鈕：`開始`、`跳過這次`。
   - 最後一次休息：右欄標籤改成紅色「最後一次」(不另加整條警告條)。按跳過要二次確認「跳過 = 今天不合格」。
2. **示範**(每個動作開始前):大畫面循環播放示範片 + 動作名稱 + 1–3 條要點 + 目標「第 1/4 組」「8–15 下」(兩個分開的字塊)。倒數 `demoSec` 秒(預設 8,設定可調 5–10),主按鈕 `開始`(內含倒數)可提前開始。
   - 設定「示範」(`showDemo`,預設開)關掉時：跳過所有示範倒數與腹肌循環的 5 秒「下一個」預覽，開場後直接進「換你做」/ 30 秒倒數。左邊示範片照樣在原位循環播放，版面不變；設定頁的「示範秒數」停用。
3. **換你做**:
   - 次數型：示範片在左邊原位置、原大小繼續循環播放(不重新載入);右欄大字顯示目標次數與正計時碼錶；提示「3–5 秒/下」。大按鈕 `完成這組`(空白鍵/Enter 也可)。
   - 秒數型(腹肌循環):30 秒倒數，歸零自動進下一個。
4. **組間休息**:倒數 `setRestSec`(預設 60,依 plan)。按鈕 `跳過休息`、`+30 秒`。左邊示範片換成下一組/下一個動作(循環播放)。可以在這裡用 −/+ 修正「上一組做了幾下」(預設 = 目標下限)。
5. **腹肌循環**:8 個動作每個 30 秒，動作之間**不休息**,只在每個動作前有 5 秒「下一個：X」預覽(示範片)。同一次休息要做 2 輪時，輪間休息 `roundRestSec`(180 秒)。中途離開 → 該輪不算完成、整輪滾到下次。
6. **完成**:顯示這次完成的組數 + 今天總進度(例如 17 / 25 組)+ 下次休息時間，5 秒後自動關閉。
7. 任何時候：`離開`(Esc)→ 確認 → 已做完的組數記錄下來，沒做完的滾到下一次。
8. **空白鍵(或 Enter)= 右下主按鈕**,每個階段都一樣：開場 `開始`、示範 `開始`、換你做 `完成這組`、組間/輪間休息 `跳過休息`、預覽 `開始`、完成 `關閉`(秒數型動作沒有主按鈕，按了不動)。每個主按鈕都顯示 `Space` 鍵帽。按住不放不會連跳：忽略按鍵自動重複，且每次換階段後 400 ms 內不接受。確認視窗打開時焦點在「繼續」,空白鍵/Enter 只按目前焦點的按鈕，不會誤按「離開 / 跳過」。

聲音：倒數最後 3 秒短嗶、換階段一聲(WebAudio 產生，不用音檔)。

## 6. 當天判定

- `pass`:所有 unit 的組數都做完。
- `fail`:最後一次休息結束(完成/離開/跳過)後還有剩；或換日時還有剩；或 active 訓練日程式整天沒開(看月曆時補判)。
- `rest`:循環休息日。`off`:非 active weekday。`pending`:今天進行中。

## 7. 資料格式(data.json)

```
{ "version": 1,
  "settings": { start, end, interval, activeWeekdays:[1..5], demoSec, showDemo, theme:"dark|light", notifyEmptySlots, autoLaunch, cycleAnchor:{date,index}, overrides:{ "<unitId>": {sets, reps:[a,b]} } },
  "days": { "YYYY-MM-DD": {
      "planDay": "d1|d2|d3|rest|off",
      "units": [ { id, name, targetSets, doneSets, reps:[實際次數...], slot } ],
      "slots": [ { time:"11:00", status:"pending|done|partial|skipped|missed|empty|notified" } ],
      "events": [ { t:"ISO", type:"break_start|set_done|skip|abort|break_end|manual", detail } ],
      "status": "pending|pass|fail|rest|off",
      "note": "" } } }
```

設定頁可覆寫每個動作的組數/次數(影片說做不完可以減量)。覆寫只影響之後新建立的日子。

## 8. 主視窗(托盤點開)

三個分頁：

- **今天**:今天是第幾天 + 主視覺(下一個要做動作的那一站:循環示範片、時間、「還有 N 分」、動作名稱與組數 × 次數;今天沒有要做的就顯示「今天完成 / 今天結束 / 今天休息」+ 下一個訓練日)+ 一天的路線圖(每一站的時間與狀態:完成 / 下一站 / 之後 / 錯過,滑過或聚焦顯示該站動作;窄視窗改成直向列表)+ 進度「7 / 25 組」+ `現在就休息` 按鈕 + 「動作示範庫」(所有動作的示範片格狀排列，點開放大循環播放，讓使用者確認動作)。
- **記錄**(tradervue 風格):
  - 上方：目前連續合格天數當主視覺(「連續 4 天」),合格率、最長連續、本月完成組數排成一行小字。
  - 月曆(可切上/下月):每格顯示日期、第幾天、完成 % ;`pass` 強調色(深色 = 綠、淺色 = 橘)、`fail` 紅、`rest`/`off` 灰、未來空白。顏色只用強調色 + 一個紅色(失敗)。
  - 每日完成率長條圖(最近 30 個訓練日，inline SVG,不用圖表套件)。
  - 點某天 → 右側明細：每個動作 已做/目標 組數與各組次數、當天每次休息的時間與結果、筆記欄(自動存)。
- **設定**:第 1、2、7 節的所有設定 + 配色(深色 / 淺色，即時套用到主視窗、休息畫面、其他螢幕覆蓋層；深色 = 綠色強調，淺色 = 白底 + 橘色強調，見 DESIGN.md §9)+ 示範開關 + 開機自啟 + 「把今天設成第幾天」+ 菜單組數/次數覆寫 + 「預覽休息畫面」按鈕(立刻開一次 overlay,不記錄)。

### 托盤(Windows 右下角通知區)—— 主要操作入口

- 圖示：程式產生的 PNG(16/32px 清楚可辨，不用 emoji)。兩種狀態：正常(強調色)、暫停中(灰)。
- tooltip:「BreakFit」「今天 17/25 組」「下次 15:00」各一行(休息日顯示「今天休息」,暫停顯示「暫停到 14:30」)。每分鐘更新。
- 左鍵單擊：打開/叫出主視窗(今天分頁)。
- 右鍵選單(每次打開時即時更新文字):
  1. 〔不可點的標題〕第 2 天　背、肩、二頭與臀腿;下一行 今天 17/25 組
  2. 〔不可點〕下次休息 15:00
  3. 現在就休息
  4. 暫停提醒 → 子選單：30 分鐘 / 1 小時 / 2 小時 / 到明天(「到明天」要確認：今天沒做完就不合格)/ 取消暫停(暫停中才出現)
  5. 打開主視窗
  6. 記錄
  7. 設定
  8. 開機啟動(checkbox)
  9. 結束
- 暫停期間到點的 slot 標成 `missed`,量照規則滾到暫停結束後的下一次休息；**最後一個 slot 不受暫停影響**(一定會跳)——除非是「暫停到明天」。
- 第一次啟動時跳一次 Windows 通知：「BreakFit 在右下角執行中」,讓使用者知道圖示在哪(圖示可能被收進 ^ 溢位區)。

## 9. 測試與自驗(必做)

- `npm test` → `node --test`,涵蓋：slot 計算(預設、邊界、interval 過大)、循環 index(跨週末)、分配公式、pending 滾動(跳過/中途離開/錯過/最後一次全清)、判定、日期換日。
- `npm run selftest`:以 `--selftest` 啟動，假時鐘，overlay 用 1280×720 視窗(不要全螢幕、不要搶焦點),自動走過：開場 → 示範 → 換你做 → 組間休息 → 腹肌循環 → 完成 → 最後一次警告；主視窗三個分頁(記錄頁先灌 45 天假資料，有 pass/fail/rest/off)。每個畫面 `webContents.capturePage()` 存成 `selftest-out/*.png`,然後結束。**selftest 不得寫入真正的 data.json**(用暫存 userData 路徑)。
- 加一個 `--fast` 旗標：1 分鐘當 1 秒，用來人工測時間表。
